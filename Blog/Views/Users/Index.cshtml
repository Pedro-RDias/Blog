@model IEnumerable<User>

@{
    ViewData["Title"] = "Users";
}

<div class="container mt-5">
    <h1 class="text-center">@ViewData["Title"]</h1>

    <p class="text-end">
        <a asp-action="Create" class="btn btn-dark text-white fw-bold">Add New User</a>
    </p>

    <div class="table-responsive bg-white rounded shadow p-4">
        <table class="table mb-0 table-borderless">
            <thead class="border-bottom">
                <tr class="text-secondary" style="color: #333; font-size: 0.9rem;">
                    <th class="py-3">First Name</th>
                    <th class="py-3">Last Name</th>
                    <th class="py-3">Username</th>
                    <th class="py-3">Phone Number</th>
                    <th class="py-3">Email</th>
                    <th class="py-3 text-end">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="border-bottom align-middle" style="background-color: #f7f7f7;">
                        <td class="py-3">@Html.DisplayFor(modelItem => item.FirstName)</td>
                        <td class="py-3">@Html.DisplayFor(modelItem => item.LastName)</td>
                        <td class="py-3">@Html.DisplayFor(modelItem => item.UserName)</td>
                        <td class="py-3">@Html.DisplayFor(modelItem => item.PhoneNumber)</td>
                        <td class="py-3">@Html.DisplayFor(modelItem => item.Email)</td>
                        <td class="py-3 text-end">
                            <div class="d-flex justify-content-end gap-2">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning text-white fw-bold">Edit</a>
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-success fw-bold text-white">Details</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger fw-bold text-white">Delete</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
